# Weather source (sources/weather.py) — owner: fill in name
WEATHER_API_KEY=

# Air quality source (sources/air_quality.py) — owner: fill in name
# Only needed if using AirNow.gov. Not required for Open-Meteo (no key needed).
AIR_QUALITY_API_KEY=

# Geocoding (location.py) — OpenWeatherMap Geocoding API
GEOCODING_API_KEY=

# Daylight source (sources/daylight.py)
# sunrise-sunset.org does not require a key — no variable needed here